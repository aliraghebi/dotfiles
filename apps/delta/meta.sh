#!/usr/bin/env bash
APP_OS="macos,linux"
APP_BINARY="delta"
APP_DESCRIPTION="delta — syntax-highlighting pager for git, diff and grep"
APP_DEPS=()
# Configured through the [delta] section of gitconfig — delta has no config file.
APP_CONFIGS=()
